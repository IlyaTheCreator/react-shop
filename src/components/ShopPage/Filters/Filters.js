import React from 'react'
import './Filters.css'

const Filters = () => {

    return (
        <div className="shop-filters">
            <span className="filter">filter 1</span>
            <span className="filter">filter 2</span>
            <span className="filter">filter 3</span>
        </div>
    )
}

export default Filters